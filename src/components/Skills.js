import React from 'react';
import styled from 'styled-components';

export default function Skills(props){
   return (
      <React.Fragment>
         <h1 id={props.id}>Skills</h1>
         <p> Thats what I know to do. =) </p>
      </React.Fragment>
   );
}

